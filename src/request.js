import { Auth } from 'components_fe'

const PERIFXS = {
    "main": ""
}

const URLS = {
    "main": [
        // ['name', 'method', 'address']
    ]
}

export const $request = Auth.request(PERIFXS, URLS)

